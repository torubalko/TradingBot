# TradingBot - Визуализатор биржевого стакана Binance

## Описание

**TradingBot** — это высокочастотное Windows-приложение на C++ для визуализации биржевого стакана (order book) и потока сделок с биржи Binance в режиме реального времени.

### Основные возможности

- 📊 **Визуализация биржевого стакана** с агрегацией по уровням цен
- 💹 **Анимация сделок** в виде "пузырьков" с физикой (spring physics)
- 🔄 **Поддержка Spot и Futures** рынков Binance
- 🎯 **Динамическое масштабирование** биржевого стакана (колесо мыши)
- 🔍 **Поиск торговых пар** с интерактивным dropdown
- ⚡ **Низкая задержка** благодаря WebSocket подключению (100ms updates)

### Технологии

- **C++17** с Visual Studio 2022
- **Boost.Beast** - HTTP и WebSocket клиент
- **Direct3D11/Direct2D** - графика и UI
- **Boost.PropertyTree & nlohmann/json** - парсинг JSON
- **OpenSSL** - SSL/TLS соединения

## Архитектура

Проект состоит из двух модулей:

1. **TradingBot.Core** - бизнес-логика:
   - Подключение к Binance API (REST + WebSocket)
   - Управление состоянием биржевого стакана
   - Обработка потока сделок

2. **TradingBot.Runner** - UI приложение:
   - Визуализация данных через Direct3D11
   - Обработка пользовательского ввода
   - Анимация trade bubbles

## Документация

- [CODE_REVIEW.md](CODE_REVIEW.md) - Детальный обзор кода, оценка качества и рекомендации
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура проекта, диаграммы и технические детали

## Сборка проекта

### Требования

- Windows 10/11
- Visual Studio 2022 или новее
- Boost 1.82+
- OpenSSL
- nlohmann/json

### Шаги сборки

1. Клонировать репозиторий:
```bash
git clone https://github.com/torubalko/TradingBot.git
```

2. Установить зависимости через vcpkg:
```bash
vcpkg install boost-beast boost-asio boost-property-tree nlohmann-json openssl
```

3. Открыть `TradingBot.slnx` в Visual Studio 2022

4. Собрать проект (F7)

5. Запустить `TradingBot.Runner`

## Использование

1. **Запуск приложения** - откроется окно с биржевым стаканом BTC/USDT
2. **Переключение рынка** - кликнуть на "SPOT" или "FUTURES" вверху
3. **Выбор пары** - кликнуть на dropdown с названием пары, ввести поиск
4. **Масштабирование** - крутить колесо мыши для изменения детализации
5. **Наблюдение за сделками** - пузырьки показывают объем и направление сделок
   - 🟢 Зеленые = покупки (market buy)
   - 🔴 Красные = продажи (market sell)

## Ключевые особенности кода

### Многопоточность
```cpp
// Network Thread - получение данных
void NetworkThreadFunc() {
    // RestAPI -> ExchangeInfo
    // WebSocket -> @depth + @trade streams
}

// Main Thread - рендеринг 60 FPS
while (running) {
    auto snapshot = g_SharedState->GetSnapshotForRender();
    RenderOrderBook(snapshot);
    RenderTradeBubbles(snapshot.trades);
}
```

### Потокобезопасность
```cpp
class SharedState {
    std::mutex mutex_;
    std::map<double, double> Bids;  // price -> quantity
    std::map<double, double> Asks;
    
    void ApplyUpdate(...) {
        std::lock_guard<std::mutex> lock(mutex_);
        // thread-safe updates
    }
};
```

### Физика анимации
```cpp
void UpdateBubbles(...) {
    const float tension = 0.4f;
    const float dampening = 0.75f;
    
    bubble.velocityRadius += (target - current) * tension;
    bubble.velocityRadius *= dampening;
    bubble.currentRadius += bubble.velocityRadius;
}
```

## Известные проблемы

⚠️ **КРИТИЧЕСКОЕ**: В `BinanceConnection` отключена верификация SSL сертификатов:
```cpp
ctx_.set_verify_mode(boost::asio::ssl::verify_none); // НЕБЕЗОПАСНО!
```

Это создает уязвимость для MITM атак. Рекомендуется включить верификацию как в `RestClient`.

Другие проблемы описаны в [CODE_REVIEW.md](CODE_REVIEW.md).

## Производительность

- **FPS**: 60 (ограничено VSync)
- **Частота обновлений**: 100ms (Binance @depth stream)
- **Потребление памяти**: ~50MB
- **CPU**: < 5% на современных процессорах

## Лицензия

MIT License - см. LICENSE файл

## Авторы

- **torubalko** - основной разработчик

## Контакты

GitHub: [torubalko/TradingBot](https://github.com/torubalko/TradingBot)

---

*Последнее обновление: 2026-01-01*
