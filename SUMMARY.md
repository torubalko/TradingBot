# Краткий обзор кода TradingBot

## Что это за проект?

**TradingBot** - это приложение на C++ для Windows, которое показывает биржевой стакан (order book) и поток сделок с биржи Binance в режиме реального времени с красивой визуализацией.

## Основная структура

### 1. TradingBot.Core (Ядро)
Бизнес-логика и работа с сетью:

- **BinanceConnection.cpp/h** - подключается к Binance через WebSocket
  - Получает обновления стакана каждые 100ms
  - Получает поток выполненных сделок
  - Использует Boost.Beast для HTTP/WebSocket

- **RestClient.cpp/h** - загружает список всех торговых пар через REST API
  - Получает информацию о tickSize (минимальный шаг цены)
  - Поддерживает Spot и Futures рынки

- **SharedState.h** - хранилище данных с защитой от гонки потоков
  - Биржевой стакан (Bids/Asks) в `std::map<double, double>`
  - Очередь последних 1000 сделок
  - Потокобезопасный доступ через мьютексы

- **JsonParser.cpp/h** - парсит JSON ответы от Binance
  - Использует Boost.PropertyTree

### 2. TradingBot.Runner (UI)
Графический интерфейс:

- **TradingBot.Runner.cpp** - главный файл (445 строк)
  - Создает окно Windows
  - Запускает два потока:
    - Network Thread - получает данные
    - Rendering Thread - рисует 60 FPS
  
- **Graphics.cpp/h** - обертка над Direct3D11/Direct2D
  - Рисует прямоугольники (уровни стакана)
  - Рисует эллипсы (пузырьки сделок)
  - Выводит текст через DirectWrite

## Как это работает?

### Поток данных:

```
1. При запуске:
   RestClient → загружает список пар (BTCUSDT, ETHUSDT, ...)
   
2. Пользователь выбирает пару (например BTCUSDT)
   
3. BinanceConnection:
   - Загружает snapshot стакана (REST)
   - Подключается к WebSocket
   - Слушает два стрима:
     * @depth - обновления стакана
     * @trade - выполненные сделки
     
4. Данные попадают в SharedState (thread-safe)

5. Каждый кадр (60 FPS):
   - Берем snapshot данных
   - Рисуем биржевой стакан (синие биды, красные аски)
   - Рисуем "пузырьки" сделок с анимацией
```

### Визуализация:

**Биржевой стакан:**
- Синие полосы слева = ордера на покупку (bids)
- Красные полосы справа = ордера на продажу (asks)
- Ширина полосы = объем на этом уровне цены
- Оранжевая линия по центру = текущая цена

**Пузырьки сделок:**
- Зеленые = market buy (покупатель инициатор)
- Красные = market sell (продавец инициатор)
- Размер пузырька = объем сделки
- Пузырьки плавно двигаются вправо и исчезают
- Используется spring physics для анимации

**Фишки:**
- Колесо мыши = масштабирование стакана (1x, 2x, 5x, 10x...)
- Поиск пар через dropdown
- Переключение Spot ↔ Futures
- Процентная шкала справа

## Оценка кода

### ✅ Что хорошо:

1. **Архитектура:**
   - Правильное разделение Core и UI
   - Потокобезопасность через мьютексы
   - Два независимых потока (network + rendering)

2. **Производительность:**
   - Батчинг графических примитивов
   - Эффективные структуры данных (std::map для O(log n))
   - Ограничение размера очередей

3. **Визуализация:**
   - Отличная физика анимации пузырьков
   - Плавное следование камеры за ценой
   - Адаптивное масштабирование

### ⚠️ Проблемы:

1. **КРИТИЧЕСКОЕ - Безопасность:**
   ```cpp
   // BinanceConnection.cpp, строка 16
   ctx_.set_verify_mode(boost::asio::ssl::verify_none);
   ```
   🔴 Отключена проверка SSL сертификатов!
   → Уязвимость для MITM атак
   → Нужно включить как в RestClient

2. **Обработка ошибок:**
   ```cpp
   catch (...) {}  // Пустые catch блоки в 5+ местах
   ```
   ⚠️ Ошибки молча игнорируются
   → Сложно отлаживать проблемы
   → Нужно добавить логирование

3. **Магические числа:**
   ```cpp
   if (g_ActiveBubbles.size() > 500) // Что за 500?
   const float tension = 0.4f;        // Почему 0.4?
   ```
   → Вынести в константы с понятными именами

4. **Длинная функция:**
   - `WinMain()` = 445 строк
   → Разбить на более мелкие функции

### 📈 Рекомендации:

**Срочно:**
1. Включить SSL верификацию
2. Добавить логирование (например, spdlog)
3. Вынести константы в Config.h

**Желательно:**
4. Написать unit-тесты (Google Test)
5. Добавить обработку переподключений
6. Документировать код (Doxygen)

**Возможно позже:**
7. Рефакторинг WinMain
8. CI/CD pipeline
9. Профилирование и оптимизация

## Итоговая оценка

**7.5 / 10**

- ✅ Архитектура: 8/10
- ✅ Производительность: 8/10
- ⚠️ Безопасность: 4/10 (SSL!)
- ✅ Код-стиль: 7/10
- ⚠️ Документация: 5/10

**Вывод:** Хороший проект с качественной визуализацией. Главная проблема - безопасность (SSL). После исправления критических замечаний готов к использованию.

## Что почитать дальше?

- **CODE_REVIEW.md** - подробный разбор каждого файла с примерами кода
- **ARCHITECTURE.md** - диаграммы архитектуры и технические детали
- **README.md** - инструкции по сборке и использованию

---

*Обзор выполнен GitHub Copilot, 2026-01-01*
