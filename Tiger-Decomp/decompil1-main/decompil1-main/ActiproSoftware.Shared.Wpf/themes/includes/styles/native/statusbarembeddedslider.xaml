<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controlsPrimitives="clr-namespace:ActiproSoftware.Windows.Controls.Primitives" xmlns:themes="clr-namespace:ActiproSoftware.Windows.Themes">
  <Style x:Key="SliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true" />
    <Setter Property="IsTabStop" Value="false" />
    <Setter Property="Focusable" Value="false" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Rectangle Fill="#00FFFFFF" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="StatusBarEmbeddedSliderButtonStyle" TargetType="{x:Type ButtonBase}">
    <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalBackgroundNormalBrushKey}}" />
    <Setter Property="Height" Value="16" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="Margin" Value="4,0" />
    <Setter Property="Padding" Value="0,1,0,0" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Width" Value="15" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ButtonBase}">
          <Border Background="#00FFFFFF" Padding="{TemplateBinding Control.Padding}">
            <ContentPresenter Name="content" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" SnapsToDevicePixels="True" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsKeyboardFocused" Value="True">
              <Setter Value="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalHighlightNormalBrushKey}}" Property="Foreground" />
            </Trigger>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter Value="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalHighlightNormalBrushKey}}" Property="Foreground" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Value="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalHighlightNormalBrushKey}}" Property="Foreground" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="UIElement.Opacity" Value="0.6" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Static themes:SharedResourceKeys.StatusBarEmbeddedSliderDecreaseButtonBaseStyleKey}" TargetType="{x:Type ButtonBase}" BasedOn="{StaticResource StatusBarEmbeddedSliderButtonStyle}">
    <Setter Property="Command" Value="{x:Static Slider.DecreaseSmall}" />
    <Setter Property="ContentTemplate" Value="{DynamicResource {x:Static themes:SharedResourceKeys.SmallMinusGlyphTemplateKey}}" />
  </Style>
  <Style x:Key="{x:Static themes:SharedResourceKeys.StatusBarEmbeddedSliderIncreaseButtonBaseStyleKey}" TargetType="{x:Type ButtonBase}" BasedOn="{StaticResource StatusBarEmbeddedSliderButtonStyle}">
    <Setter Property="Command" Value="{x:Static Slider.IncreaseSmall}" />
    <Setter Property="ContentTemplate" Value="{DynamicResource {x:Static themes:SharedResourceKeys.SmallPlusGlyphTemplateKey}}" />
  </Style>
  <Style x:Key="StatusBarHorizontalSliderThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true" />
    <Setter Property="Height" Value="15" />
    <Setter Property="Width" Value="11" />
    <Setter Property="Padding" Value="3,2" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Border Background="#00FFFFFF" Padding="{TemplateBinding Control.Padding}">
            <Rectangle Name="background" Fill="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalBackgroundNormalBrushKey}}" SnapsToDevicePixels="True" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="background" Value="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalHighlightNormalBrushKey}}" Property="Shape.Fill" />
            </Trigger>
            <Trigger Property="Thumb.IsDragging" Value="True">
              <Setter TargetName="background" Value="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalHighlightNormalBrushKey}}" Property="Shape.Fill" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="UIElement.Opacity" Value="0.6" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Static themes:SharedResourceKeys.StatusBarEmbeddedSliderStyleKey}" TargetType="{x:Type Slider}">
    <Setter Property="Background" Value="#00FFFFFF" />
    <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderThumbHorizontalBorderNormalBrushKey}}" />
    <Setter Property="Margin" Value="0,1,0,0" />
    <Setter Property="MinHeight" Value="15" />
    <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Slider}">
          <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" SnapsToDevicePixels="true">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" MinHeight="{TemplateBinding FrameworkElement.MinHeight}" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <TickBar Name="topTick" Grid.Row="0" Placement="Top" Height="4" Fill="{TemplateBinding Control.Foreground}" Visibility="Collapsed" />
              <TickBar Name="bottomTick" Grid.Row="2" Placement="Bottom" Height="4" Fill="{TemplateBinding Control.Foreground}" Visibility="Collapsed" />
              <controlsPrimitives:ElementChrome x:Name="trackBackground" Grid.Row="1" VerticalAlignment="Center" Height="1" Background="{DynamicResource {x:Static themes:AssetResourceKeys.StatusBarSliderTrackHorizontalBackgroundNormalBrushKey}}">
                <Canvas Margin="-2">
                  <Rectangle Name="PART_SelectionRange" Height="4" Fill="{DynamicResource {x:Static themes:AssetResourceKeys.SliderSelectionBackgroundNormalBrushKey}}" Stroke="{DynamicResource {x:Static themes:AssetResourceKeys.SliderSelectionBorderNormalBrushKey}}" StrokeThickness="1" Visibility="Hidden" />
                </Canvas>
              </controlsPrimitives:ElementChrome>
              <Track Name="PART_Track" Grid.Row="1">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Name="thumb" Style="{StaticResource StatusBarHorizontalSliderThumbStyle}" />
                </Track.Thumb>
              </Track>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="Slider.TickPlacement" Value="TopLeft">
              <Setter TargetName="topTick" Property="Visibility" Value="Visible" />
              <Setter TargetName="trackBackground" Property="Margin" Value="0,3,0,0" />
            </Trigger>
            <Trigger Property="Slider.TickPlacement" Value="BottomRight">
              <Setter TargetName="bottomTick" Property="Visibility" Value="Visible" />
              <Setter TargetName="trackBackground" Property="Margin" Value="0,0,0,3" />
            </Trigger>
            <Trigger Property="Slider.TickPlacement" Value="Both">
              <Setter TargetName="topTick" Property="Visibility" Value="Visible" />
              <Setter TargetName="bottomTick" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="Slider.IsSelectionRangeEnabled" Value="true">
              <Setter TargetName="PART_SelectionRange" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="UIElement.IsKeyboardFocused" Value="true">
              <Setter TargetName="thumb" Property="FrameworkElement.Tag" Value="Focused" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>