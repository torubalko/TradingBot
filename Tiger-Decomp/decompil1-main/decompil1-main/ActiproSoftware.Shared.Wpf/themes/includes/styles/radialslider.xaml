<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:ActiproSoftware.Windows.Controls" xmlns:controlsPrimitives="clr-namespace:ActiproSoftware.Windows.Controls.Primitives" xmlns:themes="clr-namespace:ActiproSoftware.Windows.Themes">
  <Style x:Key="{x:Type controls:RadialSlider}" TargetType="{x:Type controls:RadialSlider}">
    <Setter Property="Background" Value="#00FFFFFF" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="RangeBase.LargeChange" Value="6" />
    <Setter Property="RangeBase.Minimum" Value="0" />
    <Setter Property="RangeBase.Maximum" Value="360" />
    <Setter Property="RangeBase.SmallChange" Value="1" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controls:RadialSlider}">
          <Grid>
            <Ellipse Fill="{TemplateBinding Control.Background}" />
            <controlsPrimitives:CircularThumb x:Name="PART_Thumb" Style="{TemplateBinding controls:RadialSlider.ThumbStyle}" ArrowAngle="{TemplateBinding controls:RadialSlider.ThumbArrowAngle}" Background="{TemplateBinding controls:RadialSlider.ThumbBackground}" PressedBackground="{TemplateBinding controls:RadialSlider.ThumbPressedBackground}" HorizontalAlignment="Center" VerticalAlignment="Top" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type controlsPrimitives:CircularThumb}" TargetType="{x:Type controlsPrimitives:CircularThumb}">
    <Setter Property="Background" Value="#00FFFFFF" />
    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:AssetResourceKeys.ContainerForegroundLowNormalBrushKey}}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <Style>
          <Setter Property="Control.Template">
            <Setter.Value>
              <ControlTemplate>
                <Grid>
                  <Ellipse Name="FocusVisualWhite" Margin="1" IsHitTestVisible="False" Stroke="#ffffff" StrokeEndLineCap="Square" StrokeDashArray="1,1" StrokeDashOffset="1.5" />
                  <Ellipse Name="FocusVisualBlack" Margin="1" IsHitTestVisible="False" Stroke="#000000" StrokeEndLineCap="Square" StrokeDashArray="1,1" StrokeDashOffset="0.5" />
                </Grid>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </Setter.Value>
    </Setter>
    <Setter Property="Height" Value="49" />
    <Setter Property="IsTabStop" Value="True" />
    <Setter Property="Padding" Value="5" />
    <Setter Property="controlsPrimitives:CircularThumb.PressedBackground" Value="#00FFFFFF" />
    <Setter Property="Width" Value="49" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type controlsPrimitives:CircularThumb}">
          <Grid Background="#00FFFFFF">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal" />
                <VisualState x:Name="PointerOver" />
                <VisualState x:Name="Pressed">
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="SemiTransparentBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0" />
                    <DoubleAnimation Storyboard.TargetName="PressedBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0" />
                    <DoubleAnimation Storyboard.TargetName="Background" Storyboard.TargetProperty="Opacity" To="0" Duration="0" />
                  </Storyboard>
                </VisualState>
                <VisualState x:Name="Disabled" />
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <Grid Margin="{TemplateBinding Control.Padding}">
              <Ellipse Name="SemiTransparentBackground" Margin="1" Fill="{TemplateBinding Control.BorderBrush}" Opacity="0.05" />
              <Ellipse Name="PressedBackground" Margin="1" Fill="{TemplateBinding controlsPrimitives:CircularThumb.PressedBackground}" Opacity="0" />
              <Ellipse Name="Background" Margin="1" Fill="{TemplateBinding Control.Background}" />
              <Ellipse Name="Border" Stroke="{TemplateBinding Control.BorderBrush}" StrokeThickness="{Binding Path=BorderThickness.Left, RelativeSource={RelativeSource TemplatedParent}}" />
            </Grid>
            <Path Name="PART_Arrow" Data="M5.5,0 L0,5.5 L11,5.5" Fill="{TemplateBinding Control.BorderBrush}" HorizontalAlignment="Center" VerticalAlignment="Top" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>