<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:colorSelection="clr-namespace:ActiproSoftware.Windows.Controls.ColorSelection">
  <DrawingBrush x:Key="CheckerBackgroundBrush" Viewport="0,0,10,10" TileMode="Tile" ViewportUnits="Absolute">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="#FFFFFFFF">
          <GeometryDrawing.Geometry>
            <RectangleGeometry Rect="0,0 100,100" />
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
        <GeometryDrawing Brush="#FFD3D3D3">
          <GeometryDrawing.Geometry>
            <GeometryGroup>
              <RectangleGeometry Rect="0,0 50,50" />
              <RectangleGeometry Rect="50,50 50,50" />
            </GeometryGroup>
          </GeometryDrawing.Geometry>
        </GeometryDrawing>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>
  <Style x:Key="SliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true" />
    <Setter Property="IsTabStop" Value="false" />
    <Setter Property="Focusable" Value="false" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type RepeatButton}">
          <Rectangle Fill="#00FFFFFF" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="HorizontalSliderThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
    <Setter Property="Background" Value="#FF000000" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Margin" Value="-5,0,-5,0" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid VerticalAlignment="Bottom">
            <Path Data="M0.5,12.5 L0.5,4 L4.5,0 L5.5,0 L9.5,4 L9.5,12.5" Stroke="#FFFFFF" StrokeThickness="1" />
            <Path Data="M1.5,11.5 L1.5,5.5 L8.5,5.5 L8.5,11.5" Stroke="#404040" Fill="#FFFFFFFF" StrokeLineJoin="Round" />
            <Path Data="M1.5,5.5 L1.5,4.5 L4.5,1.5 L5.5,1.5 L8.5,4.5 L8.5,5.5" Stroke="#404040" Fill="#FF000000" StrokeLineJoin="Round" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="VerticalSliderThumbStyle" TargetType="{x:Type Thumb}">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
    <Setter Property="Background" Value="#FF000000" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Margin" Value="0,-5,0,-5" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type Thumb}">
          <Grid HorizontalAlignment="Right">
            <FrameworkElement.LayoutTransform>
              <RotateTransform Angle="-90" CenterX="0.5" CenterY="0.5" />
            </FrameworkElement.LayoutTransform>
            <Path Data="M0.5,12.5 L0.5,4 L4.5,0 L5.5,0 L9.5,4 L9.5,12.5" Stroke="#FFFFFF" StrokeThickness="1" />
            <Path Data="M1.5,11.5 L1.5,5.5 L8.5,5.5 L8.5,11.5" Stroke="#404040" Fill="#FFFFFFFF" StrokeLineJoin="Round" />
            <Path Data="M1.5,5.5 L1.5,4.5 L4.5,1.5 L5.5,1.5 L8.5,4.5 L8.5,5.5" Stroke="#404040" Fill="#FF000000" StrokeLineJoin="Round" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type colorSelection:ColorComponentSlider}" TargetType="{x:Type colorSelection:ColorComponentSlider}">
    <Setter Property="Background" Value="#00FFFFFF" />
    <Setter Property="BorderBrush" Value="#00FFFFFF" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Slider.IsSnapToTickEnabled" Value="True" />
    <Setter Property="Slider.TickFrequency" Value="0.001" />
    <Setter Property="RangeBase.LargeChange" Value="25.0" />
    <Setter Property="RangeBase.SmallChange" Value="10.0" />
    <Setter Property="Height" Value="20" />
    <Setter Property="RangeBase.Value" Value="0.5" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type colorSelection:ColorComponentSlider}">
          <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Height="{TemplateBinding FrameworkElement.Height}">
            <Grid>
              <Border BorderBrush="#CFCFCF" BorderThickness="1" Margin="0,0,0,9" Background="{StaticResource CheckerBackgroundBrush}">
                <Rectangle Fill="{Binding Path=ActiveGradientBrush, RelativeSource={RelativeSource TemplatedParent}}" />
              </Border>
              <Track Name="PART_Track" Margin="1,0">
                <Track.DecreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.DecreaseRepeatButton>
                <Track.IncreaseRepeatButton>
                  <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                </Track.IncreaseRepeatButton>
                <Track.Thumb>
                  <Thumb Style="{StaticResource HorizontalSliderThumbStyle}" />
                </Track.Thumb>
              </Track>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="Width" Value="Auto">
              <Setter Property="MinWidth" Value="20" />
            </Trigger>
            <Trigger Property="Height" Value="Auto">
              <Setter Property="MinHeight" Value="20" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="Slider.Orientation" Value="Vertical">
        <Setter Property="Height" Value="Auto" />
        <Setter Property="Width" Value="20" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type colorSelection:ColorComponentSlider}">
              <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Height="{TemplateBinding FrameworkElement.Height}">
                <Grid>
                  <Border BorderBrush="#CFCFCF" BorderThickness="1" Margin="0,0,9,0" Background="{StaticResource CheckerBackgroundBrush}">
                    <Rectangle Name="PART_Spectrum" Fill="{Binding Path=ActiveGradientBrush, RelativeSource={RelativeSource TemplatedParent}}" />
                  </Border>
                  <Track Name="PART_Track" Margin="0,1">
                    <Track.DecreaseRepeatButton>
                      <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                      <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource SliderRepeatButtonStyle}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                      <Thumb Style="{StaticResource VerticalSliderThumbStyle}" />
                    </Track.Thumb>
                  </Track>
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="Width" Value="Auto">
                  <Setter Property="MinWidth" Value="20" />
                </Trigger>
                <Trigger Property="Height" Value="Auto">
                  <Setter Property="MinHeight" Value="20" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="UIElement.Opacity" Value="{Binding DisabledOpacity, RelativeSource={RelativeSource Self}}" />
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>